\ProvidesPackage{j}

\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{mathtools}
\usepackage{microtype}
\usepackage{nicefrac}
\usepackage{url}
\usepackage{xcolor}
\usepackage{cleveref}  % This one needs to go last

% Setup BibLaTeX
\usepackage[style=authoryear,
            date=year,
            natbib,
            maxcitenames=2,
            maxbibnames=10,
            citestyle=authoryear,
            uniquelist=false]{biblatex}
\setlength\bibitemsep{1.5\itemsep}
% No quotation marks around titles
\DeclareFieldFormat[article, book, inbook, incollection, inproceedings, misc,
                    thesis, unpublished, techreport, phdthesis]{title}{#1}
% Sort alphabetically by surname-firstname
\DeclareNameAlias{sortname}{family-given}
% No colon after 'in'
\renewcommand*{\intitlepunct}{\space}

% Solarized dark mode
\newcommand{\darkmode}{
  \pagecolor[rgb]{0,0.168,0.211}  % Background
  \color[rgb]{0.512,0.578,0.586}  % Text
}

\newcommand{\lightmode}{
  \pagecolor[HTML]{FDF6E3}  % Background
  \color[HTML]{657b83}  % Text
}

\newcommand\given{\,\vert\,}  % Conditional probability. e.g. P(x\given y)
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}

% KL div, e.g. \kl{P}{Q}
\DeclarePairedDelimiterX{\divergence}[2]{(}{)}{#1\,\delimsize\|\,#2}
\newcommand{\kl}{D_\mathrm{KL}\divergence}

\newcommand{\E}{\mathbb{E}} % Expectation

\DeclarePairedDelimiter\abs{\lvert}{\rvert}  % Absolute value. e.g. \abs{x - 1}
\DeclarePairedDelimiter\ceil{\lceil}{\rceil}  % E.g. \ceil{x}
\DeclarePairedDelimiter\floor{\lfloor}{\rfloor}  % E.g. \floor{x}

% Shorter display urls. Example usage: \jurl{github.com/j-towns/fastar}
\newcommand\jurl[1]{\href{https://#1}{\nolinkurl{#1}}}
